using GDM_Framework.Config;
using System.Diagnostics;

namespace GDM_Framework.Selenium
{
    class AutoIt
    {
        public static void Login()
        {
            //Esta línea corría un script automatizado compilado en un .exe que es necesario para loguearse en el popup de chrome
            //que no adjunté por que era un .exe
            //ProcessStartInfo psi = new ProcessStartInfo(Settings.LoginDonMario);
            //Process proc = Process.Start(psi);
            //proc.WaitForExit();
        }
    }
}
